% Item 7 latitude-profile audit author statement template v2
% STATUS: SPEC_PENDING
% This is a non-executable template.
% It does not authorize a run, workflow, tag, certificate, or mathematical claim.

\documentclass[11pt]{article}
\usepackage[T1]{fontenc}
\usepackage[margin=28mm]{geometry}
\usepackage{array}
\usepackage{enumitem}

\newcommand{\specpending}{\texttt{SPEC\_PENDING}}
\newcommand{\placeholder}[1]{\texttt{<#1>}}

\begin{document}

\begin{center}
{\Large\bfseries Item 7 Latitude-Profile Audit}\\[4pt]
{\large Author Statement Template v2}
\end{center}

\noindent\textbf{Template status:} \specpending

\medskip
\noindent
This template is reserved for the independent audit record of item 7.  The
normative item 7 specification has not yet been frozen.  Until a full
specification commit is identified below, every substantive field remains
\specpending.  Completing or signing this document does not itself authorize
any computation, GitHub Actions workflow, tag, artifact publication,
certificate, or mathematical conclusion; each such action requires separate
explicit authorization.

\section*{1. Identification}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.34\linewidth}p{0.60\linewidth}}
Repository & \texttt{cotaxxxx/geometric-dual-topology} \\
Item & \texttt{7} \\
Audit scope & latitude-profile certification \\
Template version & \texttt{2} \\
Specification status & \specpending \\
Normative specification commit & \specpending \\
Audited source commit & \specpending \\
Audit implementation commit & \specpending \\
Authorized run identifier & \specpending \\
Input manifest SHA-256 & \specpending \\
Output artifact SHA-256 & \specpending \\
Certificate SHA-256 & \specpending \\
\end{tabular}

\section*{2. Specification gate}

No audit result may be classified as PASS while the specification status is
\specpending.  Before this statement can be completed, the auditor and author
must identify one immutable specification commit and verify that it fixes, at
minimum:

\begin{enumerate}[label=\arabic*.]
\item the exact latitude domain and all endpoint conventions;
\item the certified quantity or quantities and their strict predicates;
\item the numerical representation and canonical serialization rules;
\item the source, dependency, and kernel byte pins;
\item the coverage, termination, and unresolved-state rules;
\item the accepted output schema and deterministic artifact layout;
\item the trust base and all explicitly excluded claims;
\item the separate authorization mechanism for any real run.
\end{enumerate}

\section*{3. Author attestations}

The boxes below may be marked only after the specification gate is closed.

\begin{itemize}[label={[\ ]}]
\item I reviewed the exact specification commit identified in Section 1.
\item I verified that the audited source tree and all declared dependency
      hashes match the recorded bytes.
\item I verified that the audit used no undeclared input, fallback formula,
      unpinned kernel, or approximate value in a proof decision.
\item I verified that every claimed latitude interval is covered exactly and
      that all unresolved or incomplete states are reported fail-closed.
\item I verified the stated artifact and certificate SHA-256 values from the
      delivered bytes.
\item I verified that certified conclusions, numerical observations, and
      unproved interpretations are explicitly separated.
\item I understand that this statement does not authorize a new run, tag,
      workflow dispatch, publication, or promotion of an incomplete result.
\end{itemize}

\section*{4. Result classification}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.34\linewidth}p{0.60\linewidth}}
Audit status & \specpending \\
Allowed final values & \texttt{PASS}, \texttt{FAIL}, or \texttt{INCOMPLETE} \\
Certified statement & \specpending \\
Excluded statement(s) & \specpending \\
Unresolved count & \specpending \\
Independent reproduction & \specpending \\
\end{tabular}

\medskip
\noindent
A PASS classification requires all normative checks to pass from the pinned
bytes.  Any missing specification field, missing artifact, hash mismatch,
coverage defect, nonterminal state, or unauthorized fallback forces FAIL or
INCOMPLETE as prescribed by the frozen specification.

\section*{5. Scope limitations}

\noindent
This template records an author statement only.  It is not a mathematical
proof, machine certificate, workflow authorization, tag authorization,
artifact manifest, or substitute for an independent checker.  While the
status remains \specpending, it records no completed item 7 result.

\section*{6. Signature}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.34\linewidth}p{0.60\linewidth}}
Author name & \placeholder{author-name} \\
Signature or recorded approval & \placeholder{signature-or-approval-record} \\
Date (UTC) & \placeholder{YYYY-MM-DD} \\
Reviewed commit & \specpending \\
\end{tabular}

\end{document}
