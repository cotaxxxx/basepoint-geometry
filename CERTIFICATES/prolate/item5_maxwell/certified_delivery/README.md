# Certified delivery copy

This directory preserves the delivered Python members whose original names or roles overlap the maintained PR implementation.

- `stage5a.py` is the driver that produced the certified wide-bracket endpoint, derivative, and midpoint records.
- `prolate_maxwell_arb_certificate.py` is the delivered common integration module imported by `stage5a.py`. Its standalone `main()` still documents the abandoned narrow exploratory bracket; the certified wide-bracket theorem is produced by `stage5a.py` and recorded in the parent directory.
- `phi_split_driver.py` is an earlier narrow-bracket exploratory helper retained for provenance. It is not evidence for the final Stage 5a/5b theorem.

The original member hashes and path mapping are recorded in `../DELIVERY_AUDIT.md`.
