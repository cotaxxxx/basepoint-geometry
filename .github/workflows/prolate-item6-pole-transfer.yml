name: Prolate item 6 retired pole-transfer workflow

on:
  push:
    branches:
      - agent/prolate-item6-axis
  workflow_dispatch:

concurrency:
  group: prolate-item6-pole-transfer-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; formal pole path is direct signed-angle Psi positivity."
