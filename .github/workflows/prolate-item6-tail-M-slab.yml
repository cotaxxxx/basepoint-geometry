name: Prolate item 6 retired tail-monotonicity workflow

on:
  pull_request:
    paths:
      - ".github/workflows/prolate-item6-tail-M-slab.yml"
  push:
    branches:
      - agent/prolate-item6-axis
  workflow_dispatch:

concurrency:
  group: prolate-item6-tail-M-slab-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; replaced by signed-angle tail-grid certificates."
