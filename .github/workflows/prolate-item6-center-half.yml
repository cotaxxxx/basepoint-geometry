name: Prolate item 6 retired center-half workflow

on:
  pull_request:
    paths:
      - ".github/workflows/prolate-item6-center-half.yml"
  workflow_dispatch:

concurrency:
  group: prolate-item6-center-half-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired: replaced by the signed-angle finite-grid certificate."
