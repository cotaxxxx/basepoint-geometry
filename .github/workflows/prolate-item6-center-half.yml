name: Prolate item 6 retired center-half workflow

on:
  push:
    branches:
      - agent/prolate-item6-axis
  workflow_dispatch:

concurrency:
  group: prolate-item6-center-half-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; replaced by signed-angle finite-grid certificates."
