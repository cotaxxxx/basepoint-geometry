name: Prolate item 6 retired pole-second-boundary workflow

on:
  push:
    branches:
      - agent/prolate-item6-axis
  workflow_dispatch:

concurrency:
  group: prolate-item6-pole-second-boundary-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; pole closure uses direct signed-angle and modulus certificates."
