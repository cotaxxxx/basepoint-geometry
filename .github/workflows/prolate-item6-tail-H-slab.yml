name: Prolate item 6 retired compact tail-H workflow

on:
  push:
    branches:
      - agent/prolate-item6-axis
  workflow_dispatch:

concurrency:
  group: prolate-item6-tail-H-slab-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; replaced by signed-angle tail-grid certificates."
