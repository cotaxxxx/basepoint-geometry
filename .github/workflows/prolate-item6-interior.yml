name: Prolate item 6 retired compact-interior workflow

on:
  pull_request:
    paths:
      - ".github/workflows/prolate-item6-interior.yml"
  push:
    branches:
      - agent/prolate-item6-axis
  workflow_dispatch:

concurrency:
  group: prolate-item6-interior-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; replaced by signed-angle finite-grid certificates."
