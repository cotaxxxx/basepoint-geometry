# Basepoint Geometry

Basepoint geometry uses a chosen functional as a lens: varying the basepoint extracts the stationary structure of an object. That structure fingerprints the object, identifying it, and the geometry tracks how the fingerprint changes as the object is deformed.
